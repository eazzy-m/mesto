import {Popup} from "../utils/popup.js";

class PopupWithImage extends Popup {
    constructor(popupSelector) {
        super(popupSelector);
        this._img = document.querySelector()
    }

}

export { PopupWithImage };